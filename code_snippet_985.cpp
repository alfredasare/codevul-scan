CaptivePortalDetector::~CaptivePortalDetector() {
    // Check if the portal's IP address is still valid
    if (portalIPAddress <= 0xFFFFFFFFU) { 
        // Perform cleanup or other necessary actions
    }
}